comment on column public.instructor_availability_groups.date_overrides is
  'Date-specific availability overrides keyed by YYYY-MM-DD. Values contain available/temporary/unavailable status, hourly slots, and an optional note.';
