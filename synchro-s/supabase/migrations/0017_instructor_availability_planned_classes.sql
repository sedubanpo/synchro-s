comment on column public.instructor_availability_groups.date_overrides is
  'Per-date availability overrides. Temporary overrides may include plannedClasses keyed by hourly slot for consultation-only student placement drafts.';
